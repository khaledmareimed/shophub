{
    "name": "shophub/marketplace-backend",
    "description": "ShopHub marketplace — server-rendered PHP 8.2 + MySQL monolith",
    "type": "project",
    "require": {
        "php": ">=8.2",
        "brick/math": "^0.12 || ^0.13"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "App\\Tests\\": "tests/"
        }
    },
    "config": {
        "sort-packages": true
    },
    "suggest": {
        "ext-dom": "Required to run PHPUnit / `composer install` (install OS package php-xml, e.g. php8.3-xml on Debian/Ubuntu)"
    }
}
